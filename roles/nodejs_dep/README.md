nodejs_dep
=================

Install Node.js via the NodeSource RPM repository and the `serve` package globally.

Role Variables
--------------

| Variable | Type | Description |
| --- | --- | --- |
| `nodejs_version` | int | Major Node.js version to install (default: `21`). |
