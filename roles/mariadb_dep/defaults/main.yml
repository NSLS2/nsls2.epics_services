---
# Path where the MariaDB JDBC connector JAR is installed
mariadb_connector_jar: "/usr/lib/java/mariadb-java-client.jar"

# Version to download from Maven Central (used only when RPM unavailable)
mariadb_connector_version: "3.5.8"
