---
# Standalone task file that loads shared collection defaults.
# Used by orchestration roles that need collection vars before
# the full cs_studio_phoebus role runs (e.g. for POM templates).
- name: Load shared collection defaults
  ansible.builtin.include_vars:
    file: "{{ role_path }}/../../vars/shared.yml"
