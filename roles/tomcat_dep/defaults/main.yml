---
# tomcat_version in vars/shared.yml

# Where to install Tomcat when using the tarball fallback (RPM unavailable)
tomcat_source_install_dir: "/opt/tomcat"

# Tomcat connector ports (applied to server.xml)
tomcat_http_port: 8080
tomcat_https_port: 8443
tomcat_shutdown_port: 8005
