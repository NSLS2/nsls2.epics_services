---

- name: Reload systemd daemon
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart zookeeper
  become: true
  ansible.builtin.systemd:
    name: "zookeeper"
    state: restarted
    enabled: true

- name: Restart kafka
  become: true
  ansible.builtin.systemd:
    name: "kafka"
    state: restarted
    enabled: true
