---
# defaults file for elasticsearch_dependency

epics_services_account: csstudio

# Elasticsearch version (major.minor.patch for RPM pinning).
# Change this to upgrade — the role handles versionlock automatically.
elastic_version: "8.19.12"

# Heap size for Elasticsearch JVM
elastic_heap_size: "4g"

# Cluster name
elastic_cluster_name: elasticsearch

# Port and data paths
elastic_port: 9200
elastic_data: /var/lib/elasticsearch
elastic_logs: /var/log/elasticsearch
