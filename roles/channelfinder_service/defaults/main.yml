---
# defaults file for channelfinder_service

epics_services_account: csstudio

channelfinder_install_dir: /opt/epics-tools/services/channelfinder

cf_http_port: 8080
cf_https_port: 8443
# Channel Finder version
cf_version: ChannelFinder-4.7.3

cf_java_home: "{{ java_home }}"
cf_mvn_home: "{{ mvn_home }}"

# Path to CS-Studio Phoebus products (used to derive logback path)
cs_studio_phoebus_products_dest: "/opt/css/phoebus-products"

# Path to logback configuration
phoebus_logback: "{{ cs_studio_phoebus_products_dest }}/config/logback.xml"

es_host: "localhost"
es_port: 9200

# procServ port
cf_procServ_port: 60051
