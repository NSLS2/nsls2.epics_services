---
# handlers file for recceiver_service

- name: Reload systemd daemon
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart recsync
  become: true
  ansible.builtin.systemd:
    name: recsync
    state: restarted
    enabled: true
