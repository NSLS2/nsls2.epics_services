---

- name: Reload systemd daemon
  become: true
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart phoebus_olog_webclient
  become: true
  ansible.builtin.systemd:
    name: "phoebus_olog_webclient"
    state: restarted
    enabled: true
