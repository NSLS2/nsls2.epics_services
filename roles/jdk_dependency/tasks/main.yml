---
- name: Install OpenJDK packages ({{ jdk_version }})
  become: true
  ansible.builtin.package:
    name:
      - "java-{{ jdk_version }}-openjdk"
      - "java-{{ jdk_version }}-openjdk-devel"
    state: present
